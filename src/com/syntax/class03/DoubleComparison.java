package com.syntax.class03;

public class DoubleComparison {

	public static void main(String[] args) {
		//Create a Java program and name it Double Comparison. Declare 2 double values and if value of first variable is higher then the second, print
		//“Double value __ is larger than __ .” Otherwise print...

//Create a Java program and name it Temperature Check. Create variable to store temperature. Your program should check if temperature is below 32 then it should print
//“Water will freeze with temperature __“, otherwise “Water will NOT freeze with temperature __“

		double num=60;
		double num1=40;
		if(num>num1) {
		System.out.println("Double value "+num+" is larger than "+num1);
	}else {
		System.out.println("It is not larger");
	}
	}
}